export type ButtonState = "pressed" | "enabled" | "loading" | "disabled";

export const enum EButtonVariants {
  PRIMARY = "primary",
  SECONDARY = "secondary",
}

export const enum EButtonSizes {
  SMALL = "small",
  MEDIUM = "medium",
  LARGE = "large",
}
